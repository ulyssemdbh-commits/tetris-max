import React from 'react';

const ControlsHint = () => {
    return (
        <div className="controls-hint">
            {/* Controls instructions for the player will be displayed here */}
        </div>
    );
};

export default ControlsHint;