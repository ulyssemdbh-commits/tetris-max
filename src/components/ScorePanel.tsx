import React from 'react';

const ScorePanel = () => {
    return (
        <div className="score-panel">
            {/* Score, level, and lines will be displayed here */}
        </div>
    );
};

export default ScorePanel;